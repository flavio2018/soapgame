time_score_ms = 0;
game_over = false;
// Set the interval (in game steps) between object placements
placement_interval = 60; // Place an object every 60 steps (adjust as needed)
// Create a timer variable to track the elapsed steps
placement_timer = 0; 
